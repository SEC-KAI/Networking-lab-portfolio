The two foundation labs specify classroom passwords. They are included so the commands satisfy the Packet Tracer scoring instructions.

Before publishing configuration output as portfolio evidence, replace the cleartext classroom passwords with <REDACTED>. Do not reuse these passwords on real equipment or accounts.

