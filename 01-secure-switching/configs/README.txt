These files contain the configuration required by the 11.6.1 PDF.

Important: SW-1 F0/1 requires the real MAC address of the attached PC. The PDF does not provide it. Paste SW-1.txt first, generate traffic from the PC, then follow SW-1-F0-1-static-MAC.txt.

The lab begins partially configured. These commands preserve the existing VLAN 10, 20, and 99 access assignments and add the requested security controls.

